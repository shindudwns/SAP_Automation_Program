<!-- File: Views/LoginWindow.xaml -->
<Window x:Class="SimplifyQuoter.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="200"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0"
                   VerticalAlignment="Center"
                   Margin="0,0,8,0">CompanyDB:</TextBlock>
        <TextBox x:Name="txtCompanyDb"
                 Grid.Row="0" Grid.Column="1"
                 MinWidth="150"
                 Margin="0,4"/>

        <TextBlock Grid.Row="1" Grid.Column="0"
                   VerticalAlignment="Center"
                   Margin="0,0,8,0">Username:</TextBlock>
        <TextBox x:Name="txtUserName"
                 Grid.Row="1" Grid.Column="1"
                 Margin="0,4"/>

        <TextBlock Grid.Row="2" Grid.Column="0"
                   VerticalAlignment="Center"
                   Margin="0,0,8,0">Password:</TextBlock>
        <PasswordBox x:Name="txtPassword"
                     Grid.Row="2" Grid.Column="1"
                     Margin="0,4"/>

        <StackPanel Grid.Row="3" Grid.ColumnSpan="2"
            Orientation="Horizontal"
            HorizontalAlignment="Right"
            Margin="0,12,0,0">
            <Button x:Name="BtnCancel"
            Content="Cancel"
            Width="75"
            Margin="0,0,8,0"
            IsCancel="True"
            Click="BtnCancel_Click"/>
            <Button x:Name="BtnLogin"
            Content="Login"
            Width="75"
            IsDefault="True"
            Click="BtnLogin_Click"/>
        </StackPanel>

    </Grid>
</Window>
