<!-- File: Views/SelectItemsPage.xaml -->
<UserControl x:Class="SimplifyQuoter.Views.SelectItemsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="Auto"
             Width="Auto">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Row 0: Instructions + Range entry -->
        <StackPanel Grid.Row="0"
                Orientation="Horizontal"
                Margin="0,0,0,10">
            <TextBlock Text="Type row ranges (e.g. 1-5,8) or select directly from the sheet."
                 FontWeight="Bold"/>
            <TextBlock Text=" Range "
                 Margin="20,0,0,0"
                 VerticalAlignment="Center"/>
            <TextBox x:Name="TxtRange"
               Width="150"
               Height="24"/>
            <Button Content="Apply"
              Margin="5,0,0,0"
              Click="BtnApplyRange_Click"/>
        </StackPanel>

        <!-- Row 1: Full sheet + Selected list -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- DataGrid of all rows -->
            <DataGrid x:Name="DataGridAllRows"
                AutoGenerateColumns="False"
                SelectionMode="Extended"
                SelectionUnit="FullRow"
                SelectedCellsChanged="DataGridAllRows_SelectedCellsChanged"
                GridLinesVisibility="All"
                HeadersVisibility="Column"/>

            <!-- Selected Items list -->
            <GroupBox Header="Selected Items"
                Grid.Column="1"
                Margin="10,0,0,0">
                <DataGrid x:Name="DataGridSelected"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  GridLinesVisibility="All"/>
            </GroupBox>
        </Grid>

        <!-- Row 2: Back + Next -->
        <StackPanel Grid.Row="2"
                Orientation="Horizontal"
                HorizontalAlignment="Right"
                Margin="0,10,0,0">
            <Button Content="Back"
              Width="100"
              Margin="0,0,10,0"
              Click="BtnBack_Click"/>
            <Button Content="Review &amp; Confirm"
              Width="150"
              Click="BtnNext_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
