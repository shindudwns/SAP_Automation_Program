<!-- File: Views/ReviewConfirmPage.xaml -->
<UserControl x:Class="SimplifyQuoter.Views.ReviewConfirmPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="Auto"
             Width="Auto">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Row 0: Instructions -->
        <TextBlock Grid.Row="0"
               Text="Review the Item Master Data below. When ready, click Confirm &amp; Process."
               FontSize="14"
               Margin="0,0,0,10"/>

        <!-- Row 1: Read-only DataGrid (IMD DTO fields) -->
        <DataGrid x:Name="ReviewImdGrid"
              Grid.Row="1"
              AutoGenerateColumns="False"
              CanUserResizeColumns="True"
              IsReadOnly="True"
              HeadersVisibility="Column"
              GridLinesVisibility="All" />

        <!-- Row 2: Back + Confirm -->
        <StackPanel Grid.Row="2"
                Orientation="Horizontal"
                HorizontalAlignment="Right"
                Margin="0,10,0,0">
            <Button Content="Back"
              Width="100"
              Margin="0,0,10,0"
              Click="BtnBack_Click"/>
            <Button Content="Confirm &amp; Process"
              Width="150"
              Click="BtnConfirm_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
